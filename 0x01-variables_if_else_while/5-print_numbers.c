#include <stdio.h>

/**
*main - contains the main function
*
*Return: returns something back
*/

int main(void)
{
	int i;
	
	for (i  = 0 ; i < 10; i++)
		printf("%d", i);
	printf('\n');
	return (0);
}
