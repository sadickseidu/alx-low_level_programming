#include <stdio.h>

/**
*main - contains the main function
*
*Return: returns something back
*/

int main(void)
{
	char i;
	int i;
	
	for (i  = 0 ; i < 10; i++)
		putcher(i + '0');
	for (j  = 'a' ; j <= 'f'; j++)
		putcher(j);

	putcher('\n');
	return (0);
}

